import { instance as axios } from './request'
import { setUserInfo, getUserInfo, removeUserInfo } from './user'
import { useMyNavigate } from './hook'

export { axios }

export { setUserInfo, getUserInfo, removeUserInfo }

export { useMyNavigate }
